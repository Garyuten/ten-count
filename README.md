# 10秒ピッタリゲーム

シンプルで直感的な10秒ピッタリを目指すタイミングゲームです。

## 遊び方

1. 「スタート」ボタンを押すとタイマーがスタートします
2. カウンターが0.00秒からカウントアップを開始します
3. 3秒経過すると数字が非表示になります（より難しくなります）
4. ちょうど10.00秒だと思ったタイミングで「ココ！」ボタンを押してください
5. 10秒との差に応じてスコアが表示されます
   - 10秒に近いほど高得点
   - 10秒ピッタリ（±0.1秒以内）で「すごい！ピッタリ！」と表示
   - 15秒経過すると自動的に終了（スコア0点）

## 特徴

- デジタル時計風の見やすい表示
- 3秒後にカウンターが非表示になる演出
- 直感的な操作性
- 10秒との誤差に基づくスコアリングシステム

## 技術仕様

- HTML5
- CSS3（アニメーション効果）
- JavaScript（タイマー処理・状態管理）
- Google Fonts（DS-Digital）を使用したデジタル表示

## 主な変更点（2025/05/01）

- アクションボタンの状態分岐をボタンの文字列ではなくdata-action属性で管理するように変更
- ボタンラベル（「スタート」「ココ！」「もう一度」など）をHTMLのdata属性（data-label-start等）で管理し、JavaScriptはその値を参照してラベル・aria-labelを切り替える方式に変更
- アクセシビリティ向上のため、ボタンのaria-labelも状態に応じて自動更新
- 「もう一度」ボタンの動作不具合を修正

## ファイル構成

```
ten-count/
├── index.html          # メインのHTML
├── README.md          # プロジェクト説明
├── assets/           # 静的アセット
│   ├── images/      # 画像ファイル
│   └── fonts/       # カスタムフォント
├── css/             # スタイルシート
│   └── style.css    # メインのCSS
├── js/             # JavaScriptファイル
│   ├── components/ # コンポーネント別のJS
│   └── script.js   # メインのJS
└── data/           # ローカルデータ保存用
```

## 今後の開発予定

### ランキング機能

ローカルストレージを使用したランキング機能を実装予定です：

1. スコアの保存と管理
   - ハイスコアの記録
   - 直近10回分のスコア履歴
   - 統計情報（平均スコア、最高スコアなど）

2. ランキング表示
   - トップ10のスコア表示
   - プレイヤーの順位表示
   - スコア履歴のグラフ表示

### 実装予定のファイル

- `js/components/ranking.js` - ランキング表示用コンポーネント
- `js/components/storage.js` - データ保存管理
- `css/ranking.css` - ランキング表示用スタイル
- `data/rankings.json` - ローカル開発用のランキングデータ

将来的にはサーバーサイドと連携し、グローバルランキング機能への拡張も検討しています。